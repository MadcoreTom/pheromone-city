// Traffic
export const TRAFFIC_DECAY_RATE = 0.002;

export const MAX_CAR_RENDER_COUNT = 999;

export const TIMING = {
    FACTORY_WORK_MS: 8000,
    HOME_MS: 8000,
    SHOP_SHOPPING_MS: 2500
} as const;

export const DISPLAY_WIDTH = 1400;
export const DISPLAY_HEIGHT = 900;
export const ASPECT_RATIO = DISPLAY_WIDTH / DISPLAY_HEIGHT;

export const CAR_RETRY_DELAY_MS = 100;

//
// MONEY
//
export const INCOME_TAX = 5; // money earned by the city when someone leaves work